# Install

```bash
pip install -r requirements.txt
pip install -e .
ln -s ../../pre-commit .git/hooks/pre-commit
```

# Check code with pep8

```bash
ln -s ../../pre-commit .git/hooks/pre-commit 
```
